'use strict';

/**
 * Holds error messages for the library
 * @private
 */
export const Messages = {
  TOKEN_INVALID: 'An invalid Bearer Token was provided',
  TWEET_ID_INVALID: 'An invalid Tweet ID was provided',
};
